{
  "name": "classnames",
  "version": "1.0.0",
  "description": "Return every classname in a given document, one per line.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jason Darwin",
  "license": "ISC",
  "devDependencies": {
    "cheerio": "^0.18.0",
    "request": "^2.51.0"
  }
}
